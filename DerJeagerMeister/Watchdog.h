#pragma once
#include <SDL_timer.h>

// SINGLETON

class Watchdog
{

	/*
private:
	Watchdog();
	Watchdog* m_instance;

public:
	Watchdog* GetInstance();
	Uint32 frameStart;
	Uint32 frameTime;
	*/

};
